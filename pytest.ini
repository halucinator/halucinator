[pytest]
markers =
    avatar: testing the interaction with Avatar2
    wip: transient label for tests in progress
    slow_zmq: test uses real zmq sockets/threads that may hang in CI
    needs_root: test requires root or CAP_NET_RAW (raw sockets, scapy)
    needs_qemu: test requires a built QEMU binary
